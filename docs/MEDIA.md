# Recorded media

`docs/assets/demo.gif` contains states captured from the real application with synthetic examples. The recording script saves frame captions in `demo-frames.json`. Screenshots include light, dark, and narrow-screen views. These images are not mockups.

Run `python scripts/browser_check.py --record` to replace the media with a new verified recording.
